import React from 'react'
import styled from 'styled-components'

const PrimaryNavigationWrapper = styled.nav`
  @media (min-width: 45rem) {
    & {
      order: 2;
    }
  }
`

export default PrimaryNavigationWrapper